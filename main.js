/**
 * The main function for this screep bot
 * Nothing special to see here, it updates the map,
 * calls the screep controller and then spawns new creeps.
 */
/* Test comment */
var controller = require('game_controller');
controller();
